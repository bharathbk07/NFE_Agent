You are an expert input extractor. Given unstructured text or natural language instructions from a user, extract the target URL, credentials (like username, password, or API keys), and the user journey steps.

Input message:
{input_message}

Task:
Analyze the input text and extract the required information.

Output Format:
Output MUST be a JSON object with the following keys:
- "target_url": (string) The target website URL to navigate to. If not found, leave as an empty string.
- "credentials": (object) Key-value pairs containing credentials like "username", "password", or other keys mentioned. If not found, leave as an empty object.
- "user_journey_steps": (array of strings) Ordered list of actions described in the user journey. If not found, leave as an empty list.

Do NOT include any markdown code formatting or backticks (no ```json). Output ONLY the raw JSON object.
