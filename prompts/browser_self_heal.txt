You are an expert browser automation self-healing agent.
We attempted to perform the action '{action}' on the selector '{selector}', but it failed or timed out.

Current Page URL: {current_url}
Action details: {action_details}

Here is the HTML content of the current page (or a relevant snippet):
```html
{html_content}
```

Task:
Analyze the page HTML to find the correct CSS selector for the element we want to interact with.

Guidelines:
1. Examine the element tag name, id, name, placeholder, class name, attributes, and surrounding elements.
2. Prioritize robust, unique selectors:
   - ID: `#id_name`
   - Name attribute: `input[name="element_name"]`
   - Class name: `.button-primary`
   - Combined selectors: `form#login-form input[type="password"]`
3. If the element is a button, look for text content or labels (e.g. `button:has-text("Submit")` or simple tags if unique).
4. Return a JSON object with the key 'selector' containing the correct CSS selector.

Output Format:
Return ONLY the raw JSON object. Do not include markdown block formatting, backticks, or any conversational text.
Example response:
{{
  "selector": "#new-healed-selector"
}}
