{
    "mcpServers": {
        "k6": {
            "enabled": false,
            "description": "Grafana k6 MCP (k6 x mcp). Optional — smoke/heal uses CLI k6 run for HTML reports. Set enabled true and NFE_K6_MCP=mcp only if you need MCP tools in-bot.",
            "transport": "stdio",
            "command": "k6",
            "args": [
                "x",
                "mcp"
            ],
            "env": {}
        },
        "playwright": {
            "enabled": false,
            "description": "Optional Playwright MCP for interactive browser tools. Pipeline uses in-process Playwright+CDP by default.",
            "transport": "stdio",
            "command": "npx",
            "args": [
                "-y",
                "@playwright/mcp@latest"
            ],
            "env": {}
        },
        "chrome-devtools": {
            "enabled": false,
            "description": "Optional Chrome DevTools MCP for live network/perf traces. Not used as the capture layer.",
            "transport": "stdio",
            "command": "npx",
            "args": [
                "-y",
                "chrome-devtools-mcp@latest"
            ],
            "env": {}
        }
    }
}
